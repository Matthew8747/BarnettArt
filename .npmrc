# Pin exact versions on install so lockfile and package.json never drift.
save-exact=true
# Fail installs that surface high-severity advisories early (CI also gates this).
audit-level=high
# Reproducible CI installs.
engine-strict=true
